pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        bat 'echo "build"'
      }
    }

    stage('new build') {
      steps {
        sh 'echo "new build 2"'
      }
    }

  }
}